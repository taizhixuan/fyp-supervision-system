spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/fyp_supervision?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
  jpa:
    show-sql: true

logging:
  level:
    com.fyp.supervision: DEBUG
    org.springframework.security: DEBUG
